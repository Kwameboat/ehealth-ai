{
  "name": "ehealth-ai-api",
  "private": true,
  "version": "1.0.0",
  "description": "cPanel production — API only (no Expo). Real deps are in backend/package.json",
  "scripts": {
    "start": "node server.js",
    "postinstall": "cd backend && npm install --omit=dev"
  },
  "engines": {
    "node": ">=18"
  }
}
