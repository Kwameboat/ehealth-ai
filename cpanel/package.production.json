{
  "name": "ehealth-ai-api",
  "private": true,
  "version": "1.0.0",
  "description": "cPanel production — API only (no Expo). Install API deps in backend/ via Terminal or deploy script.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
